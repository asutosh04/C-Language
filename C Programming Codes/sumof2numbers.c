#include <stdio.h>

int main()
{
    int a,b,sum;
    printf("Enter the first number=");
    scanf("%d",&a);
    printf("\nEnter the second number=");
    scanf("%d",&b);
    sum=a+b;
    printf("The sum of two numbers is %d",sum);
    return 0;
}